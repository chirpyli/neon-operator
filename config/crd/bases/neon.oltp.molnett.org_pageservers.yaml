---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: pageservers.neon.oltp.molnett.org
spec:
  group: neon.oltp.molnett.org
  names:
    kind: Pageserver
    listKind: PageserverList
    plural: pageservers
    singular: pageserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cluster
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Available")].status
      name: Available
      type: string
    - jsonPath: .status.conditions[?(@.type=="Progressing")].status
      name: Progressing
      type: string
    - jsonPath: .status.registeredWithSC
      name: SC
      type: string
    - jsonPath: .status.scAttachedShardCount
      name: Shards
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pageserver is the Schema for the pageservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Pageserver
            properties:
              bucketCredentialsSecret:
                description: Reference to a Secret containing credentials for accessing
                  a storage bucket.
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              cluster:
                description: Used to deterministically setup which storage controller
                  and broker to communicate with
                type: string
              id:
                description: |-
                  ID which the pageserver uses when registering with storage-controller
                  This ID must be unique within the cluster.
                format: int64
                type: integer
              initialSchedulingPolicy:
                default: Active
                description: |-
                  InitialSchedulingPolicy 新节点注册后的初始 SC 调度策略。
                  "Active"（默认）：立即参与全量调度。
                  "Filling"：仅接收新 shard placement（用于新节点预热）。
                enum:
                - Active
                - Filling
                type: string
              nodeFailure:
                description: NodeFailure 控制节点故障时的自动恢复策略。
                properties:
                  autoRecover:
                    description: |-
                      AutoRecover 是否在节点宕机时自动删除 PVC 并重建。
                      默认 false（需要人工确认），可设为 true 启用自动恢复。
                    type: boolean
                  maxPendingDuration:
                    description: |-
                      MaxPendingDuration 在判定 PVC 无法恢复前，Pod Pending 的最大等待时间。
                      默认 5 分钟。
                    type: string
                type: object
              resources:
                description: |-
                  Resources 指定 pageserver 容器的 CPU/内存配置。
                  若未设置，使用 operator 内置默认值。
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storageConfig:
                description: PVC configuration
                properties:
                  size:
                    description: |-
                      Size of the PVCs.
                      kubebuilder:default:=10Gi
                    type: string
                  storageClass:
                    description: Name of the storage class to use for PVCs.
                    type: string
                required:
                - size
                type: object
            required:
            - bucketCredentialsSecret
            - cluster
            - id
            - storageConfig
            type: object
          status:
            description: status defines the observed state of Pageserver
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nodeID:
                description: NodeID 是 SC 分配的节点 ID，与 Spec.ID 相同（注册后确认）。
                format: int64
                type: integer
              observedGeneration:
                format: int64
                type: integer
              registeredWithSC:
                description: RegisteredWithSC 指示 pageserver 是否已在 Storage Controller
                  中成功注册。
                type: boolean
              scAttachedShardCount:
                description: SCAttachedShardCount 当前节点作为 Attached 的 shard 数量。
                format: int32
                type: integer
              scAvailability:
                description: |-
                  SCAvailability mirrors SC's NodeAvailability
                  "Active", "WarmingUp", "Offline"
                type: string
              scSchedulingPolicy:
                description: |-
                  SCSchedulingPolicy mirrors SC's NodeSchedulingPolicy
                  "Active", "Filling", "Pause", "PauseForRestart", "Draining", "Deleting"
                type: string
              scTotalShardCount:
                description: SCTotalShardCount 当前节点上的 shard 总数（Attached + Secondary）。
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
